<html>

    <head>
        <meta charset="utf-8">
        <title>Your feet need shoes!</title>
        <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css" rel="stylesheet">

    </head>
    <body>

        <div class="container">

            <div>

                {% if stores is not empty %}
                <p>Here are some stores!</p>
                <ul>
                    {% for store in stores %}
                    <li>{{ store.getStoreName }}</li>
                    {% set brands = store.getBrands %}
                    {% for brand in brands %}
                    <ul>
                        <li>{{ brand.getBrandName }}</li>
                    </ul>
                    <br/>
                    {% endfor %}
                    {% endfor %}
            </ul>
            {% endif %}

        </div>

        <div>

            <form action='/store_brand' method='post'>
                <label for='store_name'>Shoe Store:</label>
                <input id='store_name' name='store_name' placeholder='Payless' type='text'>
                <label for='brand_name'>Brand Name:</label>
                <input id='brand_name' name='brand_name' placeholder='Nike' type='text'>

                <button type='submit'>Add</button>
            </form>

            <form action='/delete_all' method='post'>
                <button type='submit'>Clear All</button>
            </form>

            <p>
                <a href='/'>Home</a>
            </p>

        </div>

    </div>

</body>

</html>
